s.options.inDevice =  "MacBook Pro Microphone";

(
w = Window.new("player", Rect.new(
	Window.screenBounds.height/2,
	Window.screenBounds.width/2,
	500,
	500
))
.front
.alwaysOnTop_(true);
~slider = Slider.new(
)


w.close;